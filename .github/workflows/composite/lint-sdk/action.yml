name: 'Lint SDK'
description: 'Lint SDK from a given directory'
inputs:
  module-name: 
    description: 'Name of the module to lint.'
    required: true
runs:
  using: "composite"
  steps:
    - uses: actions/download-artifact@v3
    - uses: ./.github/workflows/composite/setup-ruby
    - name: Install rubocop (1.12)
      shell: bash
      run: gem update --system && gem install rubocop --version 1.12 && ruby -v
    - name: Lint module
      shell: bash
      run: cd ${{ inputs.module-name }} && rubocop --fail-level=error